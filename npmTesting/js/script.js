/* const titleEl = document.querySelector(".intro-h1");
const colors = ["red", "green", "purple", "blue"];

titleEl.addEventListener("mousemove", () => {
  for (let c of colors) {
    titleEl.style.color = c;
  }
});
 */
